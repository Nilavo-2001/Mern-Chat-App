{
  "scripts": {
    "start": "node backend/server.js",
    "build": "npm install --legacy-peer-deps && npm install --legacy-peer-deps --prefix backend && npm install --legacy-peer-deps --prefix client && npm run build --prefix client"
  },
  "dependencies": {
    "socket.io": "^4.7.2"
  }
}
